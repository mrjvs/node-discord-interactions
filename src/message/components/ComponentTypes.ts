export enum MessageComponentType {
  ACTION_ROW = 1,
  BUTTON = 2,
  SELECT_MENU = 3,
  INPUT = 4,
}

export enum MessageButtonType {
  PRIMARY = 1,
  SECONDARY = 2,
  SUCCESS = 3,
  DANGER = 4,
  LINK = 5,
}

export enum MessageInputType {
  SHORT = 1,
  PARAGRAPH = 2,
}
